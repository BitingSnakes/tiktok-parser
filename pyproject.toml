[project]
name = "shorts-scraper"
version = "0.1.0"
description = "Some scrapers to parse TikTok, Instagram reels"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "silkworm-rs>=0.6.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.20",
]
